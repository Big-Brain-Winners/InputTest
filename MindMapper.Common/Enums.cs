namespace MindMapper.Common;

public enum ChannelType
{
    Null = -1,
    Emg = 0,
    Accelerometer = 1
}

public enum ControlTypeCode
{
    none = -1,
    button = 0,
    axis = 1,
    slider = 2,
}